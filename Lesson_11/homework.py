1. learn about git ignore
2. remove sort from non repeating char, use min on dict values. implement with list instead of dict.
3. build HashMap class, with the following methods:
* set(key,value)
* get(key)-returns value
* exists(key)-returns true\false
with no dictionary, without Hash function
keys can only be strings (a-z,A-Z and digits) 
interesting prints (encounter hash colision etc')